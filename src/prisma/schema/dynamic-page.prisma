model DynamicPage {
  id          String   @id @default(cuid())
  title       String
  slug        String   @unique
  description String
  isPublished Boolean  @default(false)
  createdAt   DateTime @default(now())
}
