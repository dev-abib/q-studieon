model Question {
  id        String         @id @default(cuid())
  text      String
  options   String[]
  createdAt DateTime       @default(now())
  slug      String         @unique
  updatedAt DateTime       @updatedAt
}
