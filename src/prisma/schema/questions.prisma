model Question {
  id        String         @id @default(cuid())
  text      String
  options   String[]
  createdAt DateTime       @default(now())
  slug      String
  updatedAt DateTime       @updatedAt
}
